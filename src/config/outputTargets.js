// Global, domain-agnostic output targets. UI, serialization, and compiler
// dispatch all consume this single vocabulary.
export const OUTPUT_TARGETS = Object.freeze(['markdown', 'claude-xml', 'openai-json']);
